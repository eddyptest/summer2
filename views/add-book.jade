extends layout2
block content
   .row
   h1 welcome to the add a book page <br><br>
     .formHold
      h1.itemtitle Add New Product <br><br>
           form(method="post")
             fieldset
               label(for="name") Book Name:
               input(name="name", type="text",placeholder="enter a book name")
               label(for="price") Book Price
               input(name="price", type="text", placeholder="enter a selling price")
               label(for="image") Book Image:
               input(name="image", type="text", placeholder="what image will this product have")
               button.button(type="submit") Create New Book