extends layout2
block content
    .row
      
            each book in indi
             .ibook
              .item 
                h1.itemtitle #{book.name}
                img.pimage(src='../../#{book.image}', alt='some image')
                p.itemp Price: €#{book.price}